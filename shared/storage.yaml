classname: tsdat.io.storage.FileSystem
parameters:
  storage_root: "pacwave"
  data_storage_path: "data/{location_id}/{datastream}/{year}/{month}/{day}"
  ancillary_storage_path: "ancillary/{location_id}/{datastream}/{year}/{month}/{day}"

handler:
  classname: tsdat.io.handlers.NetCDFHandler
