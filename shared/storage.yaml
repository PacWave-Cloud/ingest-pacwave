classname: tsdat.io.storage.FileSystem
parameters:
  storage_root: "/share/upload/staging/pacwave"
  data_storage_path: "{location_id}/data/{datastream}/{year}/{month}/{day}"
  ancillary_storage_path: "{location_id}/ancillary/{datastream}/{year}/{month}/{day}"

handler:
  classname: tsdat.io.handlers.NetCDFHandler
