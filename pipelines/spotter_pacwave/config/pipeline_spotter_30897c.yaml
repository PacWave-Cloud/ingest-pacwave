classname: pipelines.vap_pacwave.pipeline.VapPacwave
parameters:
  datastreams:
    # DEVELOPER: put your input datastream(s) here. The datastream is the folder name
    # used in the storage area e.g.:
    - pacwave.spot_30897c.c1


triggers: []

retriever:
  path: pipelines/vap_pacwave/config/retriever.yaml

dataset:
  path: pipelines/vap_pacwave/config/dataset.yaml
  overrides: 
    /attrs/dataset_name: spot_30897c
    /attrs/platform_id: SPOT-30897C
    # /attrs/geospatial_lat_max: 44.6978
    # /attrs/geospatial_lat_min: 44.6110
    # /attrs/geospatial_lon_max: -124.0496
    # /attrs/geospatial_lon_min: -124.1464 // does VAP need geo fence?
    

quality:
  path: shared/quality.yaml
storage:
  path: shared/storage.yaml
