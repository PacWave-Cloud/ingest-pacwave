classname: pipelines.floatr_ctd.pipeline.FLOATrCTD

triggers:
  - .*PWS.*004.*Met.*\.dat
  - .*4S.*Met.*\.csv

retriever:
  path: pipelines/floatr_ctd/config/retriever.yaml

dataset:
  path: pipelines/floatr_ctd/config/dataset.yaml
  overrides:
    /attrs/qualifier: "004"

quality:
  path: shared/quality.yaml
storage:
  path: shared/storage.yaml
