classname: pipelines.spotter_raw.pipeline.SpotterRaw

triggers:
  - .*30903.*\.zip

retriever:
  path: pipelines/spotter_raw/config/retriever.yaml

dataset:
  path: pipelines/spotter_raw/config/dataset.yaml
  overrides:
    /attrs/qualifier: 30903C

quality:
  path: shared/quality.yaml
storage:
  path: shared/storage.yaml
